package bg.tusofia.vlp.user.domain;

/**
 * Enum: RoleType
 * <p>
 * A role type enum containing all applications user roles
 *
 * @author Ivan Simeonov
 * @since 0.0.1
 */
public enum RoleType {
    ROLE_STUDENT,
    ROLE_TEACHER,
    ROLE_ADMIN,
    ROLE_ROOT_ADMIN
}
